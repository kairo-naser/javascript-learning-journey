// this to take the input from the users 
const readline = require('readline')
const rl = readline.createInterface({
   input: process.stdin,
   output:process.stdout
})
// intialize array for grid as empty 
ticGrid=[
    ["", "", ""],
    ["", "", ""],
    ["", "", ""]
]
// function that updates the matrix grid
function placeMark(x, y, mark){
  rl.question((input)=>{})
}